<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.davidcampbell.project0.MainActivity"
    android:background="#cfcccc">

    <TextView android:id="@+id/title_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="My Nanodegree Apps!"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textAppearance="?android:attr/textAppearanceLarge" />

    <Button android:id="@+id/spotify_button"
        style="@style/Udacity"
        android:text="Spotify Streamer"
        android:layout_below="@+id/title_textView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:paddingLeft="35dp"
        android:paddingRight="35dp"
        />

    <Button android:id="@+id/scores_button"
        style="@style/Udacity"
        android:text="Scores App"
        android:layout_below="@+id/spotify_button"
        android:layout_alignStart="@+id/spotify_button"
        android:layout_alignEnd="@+id/spotify_button"
        />

    <Button android:id="@+id/library_button"
        style="@style/Udacity"
        android:text="Library App"
        android:layout_below="@+id/scores_button"
        android:layout_alignStart="@+id/spotify_button"
        android:layout_alignEnd="@+id/spotify_button"
        />

    <Button android:id="@+id/buildIt_button"
        style="@style/Udacity"
        android:text="Build it Bigger"
        android:layout_below="@+id/library_button"
        android:layout_alignStart="@+id/spotify_button"
        android:layout_alignEnd="@+id/spotify_button"
        />

    <Button android:id="@+id/xyzReader_button"
        style="@style/Udacity"
        android:text="XYZ Reader"
        android:layout_below="@+id/buildIt_button"
        android:layout_alignStart="@+id/spotify_button"
        android:layout_alignEnd="@+id/spotify_button"
        />

    <Button android:id="@+id/capstone_button"
        style="@style/Udacity"
        android:text="Capstone: My own app"
        android:background="@color/Red"
        android:layout_below="@+id/xyzReader_button"
        android:layout_alignStart="@+id/spotify_button"
        android:layout_alignEnd="@+id/spotify_button"
        />


</RelativeLayout>